services:
  - type: web
    name: maxiptv-update
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: PORT
        value: 10000
    autoDeploy: true

